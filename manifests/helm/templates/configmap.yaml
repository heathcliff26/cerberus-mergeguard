apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cerberus-mergeguard.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cerberus-mergeguard.labels" . | nindent 4 }}
data:
  config.yaml: |-
{{- .Values.config | toYaml | nindent 4 }}
